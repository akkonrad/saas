{
  "name": "koalka",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/koalka/src",
  "targets": {
    "dev": {
      "command": "astro dev",
      "options": { "cwd": "apps/koalka" }
    },
    "build": {
      "command": "astro build",
      "options": { "cwd": "apps/koalka" }
    },
    "preview": {
      "command": "astro preview",
      "options": { "cwd": "apps/koalka" }
    }
  }
}
